# Privy app id (public, baked into the bundle)
VITE_PRIVY_APP_ID=
# Derive referral code: orders carry it so Derive credits the fee share + builder fee to its receiving wallet
VITE_REFERRAL_CODE=
# Server only (never VITE_): lets /who map addresses to X handles via the Privy API
PRIVY_APP_ID=
PRIVY_APP_SECRET=
